---
interface Props {
  title: string;
  description?: string;
  icon?: string;
}
const { title, description, icon } = Astro.props;
---

<article class="page glass">
  <h1>{icon && <span set:html={icon} />}{title}</h1>
  <p>{description || '这个页面还在建设中，敬请期待。'}</p>
</article>
